# Healthcare Appointment & Follow-Up Manager - Final Release Checklist

- [x] **Environment Variables**: Formatted in `.env.example` with clear placeholders. Validated at startup via `server/src/config/env.js`.
- [x] **JWT Secret Enforcement**: Dangerous production fallbacks eliminated (`JWT_SECRET` required in production).
- [x] **SMTP Configuration**: Transport errors cleanly isolated; notification retry queue handles temporary delivery failures.
- [x] **Gemini AI Safety**: Structured JSON contract, Zod schema validation, source-grounding validator, and `needsHumanReview` flagging active.
- [x] **Google OAuth Integration**: Off-line token storage, automatic refresh token persistence, and Google Calendar event push active.
- [x] **Clean Migration Deployment**: 7 versioned Prisma migrations verified on fresh PostgreSQL database (`clean_healthcare_db`) via `npx prisma migrate deploy` with zero `prisma db push`.
- [x] **Backend Startup**: Server initializes cleanly with environment validation, Prisma connection, and `cronService` workers.
- [x] **Frontend Build**: Vite production compilation (`npm run build`) completed in 1.19s with zero errors.
- [x] **Patient E2E Journey**: Registration, slot search, ephemeral 5-min holds, double-booking defense (409 Conflict), confirmation emails, `.ics` download, Google Calendar link, and cancellation verified.
- [x] **Doctor E2E Journey**: Consultation completion, clinical notes, prescription submission, AI summary generation, grounding validation, doctor `⚠️ Pending Review` UI badge, and doctor approval endpoint verified.
- [x] **Admin E2E Journey**: Doctor management, multi-day leave range submission, auto-cancellation of conflicting bookings, patient leave notifications, and range validation (`endDate < startDate` -> 400) verified.
- [x] **Double-Booking Defense**: PostgreSQL partial unique index `unique_active_doctor_slot` prevents duplicate bookings under race conditions (201 / 409).
- [x] **Ephemeral Slot Holds**: Ephemeral `SlotHold` table reserves slots for 5 minutes and auto-purges via minute cron worker.
- [x] **AI Grounding & Prompt Injection**: Ungrounded clinical items flagged for human review; summaries hidden from patients until doctor approval.
- [x] **Human Review System**: Doctor interface displays flagged reasons and "Approve Summary" action (`POST /api/doctor/appointments/:id/approve-summary`).
- [x] **Notification Retry Queue**: Central `notificationService.js` retries failed deliveries up to 5 max attempts (+1m, +5m, +15m, +60m).
- [x] **Medication Reminder Idempotency**: 1-minute cron worker dispatches reminders idempotently using unique `eventKey` indexing.
- [x] **Calendar & ICS Sync**: `.ics` string generation and Google Calendar consent flow operational.
- [x] **Authorization & IDOR Protection**: Server-side RBAC and resource ownership checks enforced across all routes.
- [x] **Secret Scan**: Secrets excluded from codebase and stored strictly in `.env`.
- [x] **npm Audit**: Dependencies reviewed and documented.
- [x] **Git Tracking Audit**: Clean working tree; migrations tracked while `.env` and local build outputs are ignored.
